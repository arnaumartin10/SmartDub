# Core deep learning stack — CUDA 12.1 compatible (tested 2025-12)
# Install torch ecosystem via pip index URL (see README / Makefile):
#   pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 torchaudio==2.5.1+cu121 \
#       --index-url https://download.pytorch.org/whl/cu121

torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# Computer vision & media
opencv-python==4.9.0.80
ffmpeg-python==0.2.0
mediapipe==0.10.21

# Numerical computing
# IMPORTANT: torch 2.x was compiled against the NumPy 1.x ABI.
# Do NOT upgrade numpy to >=2.0 — it will produce import warnings and may crash.
numpy==1.26.4
scipy==1.14.1

# Utilities
tqdm==4.67.1
pyyaml==6.0.2

# HuggingFace ecosystem
huggingface_hub==0.26.5
transformers==4.47.1
accelerate==1.2.1
diffusers==0.32.1

# Audio processing (for forced alignment / phoneme extraction)
librosa==0.10.2.post1
soundfile==0.12.1
g2p_en==2.1.0
# WhisperX 3.3.x uses faster-whisper/CTranslate2 for ASR and wav2vec2 for alignment.
# Keep numpy pinned at 1.26.4; install this line without allowing resolver upgrades.
whisperx==3.3.1

# Testing
pytest==8.3.4
pytest-cov==6.0.0

# Development tools
black==24.10.0
isort==5.13.2
flake8==7.1.1
