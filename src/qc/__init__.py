"""
src/qc/__init__.py
───────────────────
Quality Control sub-package.

Planned modules:
  scorer.py        — Composite quality scorer (SyncNet, FID, PESQ, SSIM)
  syncnet.py       — SyncNet audio-visual sync confidence metric
  perceptual.py    — Perceptual image quality metrics (LPIPS, FID)
  reporter.py      — JSON/HTML quality report generation
"""

# Status: PLANNED — no implementations yet
